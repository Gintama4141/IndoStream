// use an integer for version numbers
version = 13 // Sebaiknya naikkan versi ini ke 14 setelah perubahan

cloudstream {
    language = "id"
    authors = listOf("Hexated", "TeKuma25")
    status = 1 
    tvTypes = listOf(
        "AnimeMovie",
        "OVA",
        "Anime",
    )

    // ================== PERUBAHAN DI SINI ==================
    iconUrl = "https://www.google.com/s2/favicons?domain=anime-indo.lol&sz=%size%"
    // =======================================================
}
